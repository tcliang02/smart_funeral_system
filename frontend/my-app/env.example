# ============================================
# Supabase Database Configuration
# ============================================
# These variables are used in Next.js API routes (server-side only)

# OPTION 1: Connection String (RECOMMENDED)
# Get this from Supabase Dashboard → Settings → Database → Connection String
# Format: postgres://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgres://postgres:your-password@db.wtfngwbynkkmtjcsdqnw.supabase.co:5432/postgres

# OPTION 2: Individual Variables (Alternative if DATABASE_URL doesn't work)
# Uncomment these if you prefer individual variables over connection string
# DB_HOST=db.wtfngwbynkkmtjcsdqnw.supabase.co
# DB_USER=postgres
# DB_PASSWORD=your-supabase-password-here
# DB_NAME=postgres
# DB_PORT=5432

# For Production (Vercel), consider using Connection Pooler:
# DB_HOST=aws-0-[region].pooler.supabase.com
# DB_PORT=6543

# ============================================
# Authentication
# ============================================
# JWT Secret for authentication tokens
# Generate a strong random string for production (use: openssl rand -base64 32)
JWT_SECRET=your-very-long-random-secret-key-here

# ============================================
# Supabase Client Configuration
# ============================================
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# These are used in React components and client-side code

# Supabase Project URL
# Get from: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://wtfngwbynkkmtjcsdqnw.supabase.co

# Supabase Anonymous Key (public, safe to expose)
# Get from: Supabase Dashboard → Settings → API → anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Supabase Service Role Key (SECRET - server-side only, never expose to browser!)
# Get from: Supabase Dashboard → Settings → API → service_role key
# ⚠️ WARNING: This key bypasses Row Level Security (RLS) - keep it secret!
# Used for: Admin operations, file uploads, server-side operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file to .env.local (it's gitignored)
#    Command: cp env.example .env.local
#
# 2. Replace all placeholder values with your actual Supabase credentials
#    Get credentials from: https://supabase.com/dashboard/project/[YOUR-PROJECT-REF]
#
# 3. For Vercel deployment:
#    - Go to Vercel Dashboard → Your Project → Settings → Environment Variables
#    - Add all the same variables from this file
#    - Make sure to add for Production, Preview, and Development environments
#
# 4. Test connection locally:
#    - Run: npm run dev
#    - Visit: http://localhost:3000/api/backend/test-db
#    - Should return: {"success":true,"message":"Database connected"}
#
# ============================================
# Architecture
# ============================================
# Frontend + Backend: Vercel (Next.js)
# Database: Supabase (PostgreSQL)
# See: VERCEL_SUPABASE_ARCHITECTURE.md for details

